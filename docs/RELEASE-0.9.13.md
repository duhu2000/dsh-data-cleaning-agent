# 0.9.13 — 接纳任务后自动展开工作台

- 实际提交接纳后，自动展开所属 Session 的工作台，方便查看任务执行进展。
- 草稿输入与回填不展开；失败发送不伪造已启动。
- 同一任务凭证与提交回调只自动展开一次，手动收起后不会被后续轮询拉开。
- 后台会话不抢占当前会话；接纳回调观察器跟随真实绑定切换，并在卸载时释放。
- 保留 0.9.12 的统一首页、稳定菜单及真实状态提示。

无需改动业务数据、任务存储或用户 MCP 授权。
